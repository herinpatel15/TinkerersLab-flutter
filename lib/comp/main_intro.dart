import 'package:flutter/material.dart';

class MainInro extends StatelessWidget {
  const MainInro({super.key});

  @override
  Widget build(BuildContext context) {
    return Image.asset(
      'asset/mainBG.png',
      width: double.infinity,
    );
  }
}
